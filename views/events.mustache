<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>{{ ticker }}</title>
	<style>
		body { background: #333; color: #eee; font-family: 'Helvetica Neue', sans-serif; font-weight: 200; font-size: 1rem; max-width: 800px; margin:auto; padding: 10px; }
		table { width: 100%; border-collapse: collapse; }
		td, th { border: 0px solid #aaa; padding: 5px; vertical-align: top; }
		a { color: orange; }
		label { display: inline-block; white-space: nowrap; }
		input:not([type=checkbox]) { width: 40px; font-size: 1rem; text-align: left;  font-family: 'Helvetica Neue', sans-serif; font-weight: 200; color: #fff; background: none;}
		table.compact td { padding: 0; border: 0 }
		.line { height: 25px }
		.nowrap { white-space: nowrap; }
		section { max-width: 480px; margin: auto; position: relative; }
		i { font-style: normal; }
		.red { color: red; }
		.green { color: lime; }
		.gray { color: gray; }
		button { font-size: 16px; font-family: 'Helvetica Neue', sans-serif; font-weight: 400; }
		ol, li { margin: 0; padding: 0; list-style-type: number }
		ol li span { font-size: 12px; color: #888; width: 130px; display: inline-block; white-space: nowrap;}
		progress { background: green }
		.input-disabled { background: none; color: #888 !important; }
		.menu { position: fixed; z-index: 1; left: 0; bottom: 0; background:  #000; width: 100%; text-align: center; font-weight: 500; }
		.menu td { border: 1px solid #555; width: 25%; font-size: 14px; height: 30px; line-height: 30px; vertical-align: middle; }
		.income { border-top: 1px solid #888; margin-top: 10px; padding-top: 5px; }
		/*.portfolio { white-space: nowrap; }*/
		a { color: #fff; text-decoration: none; }
		h1 small { float: right; font-weight: normal; font-size: 1rem; }
		.stock-disabled a b { color: #888; text-decoration: line-through; font-weight: 400; }
		@media screen and (min-width:768px) { ol li > div { width: 500px !important } section { max-width: 800px } }
	</style>
</head>
<body>
	

	<section style="display: block;">

		<a href="/ui" style="float: right; display: block; padding: 10px;">← Back</a>
		<h1>Events</h1>





<div class="events">
				<span style="float: right;">{{dealsToday}} buys today</span>
				<h1 style="font-weight:100;">Events</h1>
				
				<ol reversed>
					{{#events}}
					<li class="">
						<span>{{ dateStr }} <em><b><a href="/ui/{{ticker}}">{{ticker}}</a></b></em></span>
						
						<div style="display: inline-block; font-size: 14px; width: 220px; vertical-align: top;">
							{{#isIncome}}
							<i class="green"><b>+$ {{value}}</b></i>
							{{/isIncome}}
							{{^isIncome}}

							{{{action}}}:

							{{#value}}
							$ {{value}}
							{{/value}}
							{{info}}
							{{/isIncome}}
						</div>
					</li>
					{{/events}}
				</ol>
				<br>
				<a href="/ui/events?from={{yesterday}}" style="text-decoration:underline;">Yesterday</a>
				<a href="/ui/events" style="text-decoration:underline;">Today</a>
			</div>


	</section>

</body>
</html>